#!/bin/bash 

#This file should deploy all parts of the application stack. 

project_path='/Users/mutzel/workspace/aws-deployments'

path="$project_path"/bin/deploy_vpc.yml

echo "executing $path"
ansible-playbook -i inventory $1 $path
#ansible-playbook -i inventory $1 ${project_path}/bin/deploy_az.yml  &
#ansible-playbook -i inventory $1 ${project_path}/bin/deploy_bigip.yml